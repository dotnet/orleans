name: Build and publish site
on:
  workflow_dispatch:
  push:
    branches:
      - docs

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        ref: 'docs'
    - name: Setup .NET Core 3.1
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '3.1.x'
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
    - name: Build
      run: dotnet build src
    - name: Publish
      run: |
        #!/bin/bash
        
        cd _site
        
        echo remote_repo="https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
        $remote_branch="gh-bages"
        
        git init
        git config user.name "${GITHUB_ACTOR}"
        git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
        git add .
        
        echo git commit -m "GitHub Action - Automated build"
        echo git push --force $remote_repo master:$remote_branch
      

<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <PackageId>Microsoft.Orleans.LegacyOrleansLoggerProvider</PackageId>
    <Title>Microsoft Legacy Orleans Logger Provider</Title>
    <Description>Library of LegacyOrleansLoggerProvider.</Description>
    <PackageTags>$(PackageTags) Orleans</PackageTags>
  </PropertyGroup>

  <PropertyGroup>
    <AssemblyName>LegacyOrleansLoggerProvider</AssemblyName>
    <RootNamespace>Orleans.Extensions.Logging</RootNamespace>
    <PackageTargetFallback Condition=" '$(BuildFlavor)' == 'Legacy' ">$(PackageTargetFallback);portable-net45+win8+wpa81+wp8</PackageTargetFallback>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Orleans\Orleans.csproj" />
  </ItemGroup>
</Project>

<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <PackageId>Microsoft.Orleans.Reminders.GoogleCloudStorage</PackageId>
    <Title>Microsoft Orleans Google Cloud Storage Reminders Provider</Title>
    <Description>Microsoft Orleans reminders provider backed by Google Cloud Storage</Description>
    <PackageTags>$(PackageTags) Google Cloud Storage</PackageTags>
    <TargetFrameworks>$(DefaultTargetFrameworks)</TargetFrameworks>
    <AssemblyName>Orleans.Reminders.GoogleCloudStorage</AssemblyName>
    <RootNamespace>Orleans.Reminders.GoogleCloudStorage</RootNamespace>
    <OrleansBuildTimeCodeGen>true</OrleansBuildTimeCodeGen>
    <DefineConstants>$(DefineConstants);ORLEANS_REMINDERS</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="$(SourceRoot)src\Orleans.Runtime\Orleans.Runtime.csproj" />
    <ProjectReference Include="$(SourceRoot)src\Orleans.Reminders\Orleans.Reminders.csproj" />
    <PackageReference Include="Google.Cloud.Storage.V1" />
    <PackageReference Include="System.Net.NameResolution" />
  </ItemGroup>
</Project>


The Log directory is created in the CURRENT DEFAULT directory as determined by the System.Environment.CurrentDirectory path in a  subdirectory based on the date. Each test
run will have its own separate sub-directory under the parent directory.

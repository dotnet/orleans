{
  "description": "Generated API assemblies which are not currently published as standalone NuGet packages.",
  "packages": {
    "Microsoft.Orleans.DurableMessaging": "The new package is awaiting its first NuGet publication.",
    "Microsoft.Orleans.DurableTasks": "The experimental durable task runtime assembly is not published as a standalone package.",
    "Orleans.DurableTasks.Abstractions": "The experimental durable task abstractions assembly is not published as a standalone package."
  }
}

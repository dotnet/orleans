{
  "name": "@dotnet/orleans-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "api:generate": "pwsh ../scripts/Generate-ApiData.ps1 -Parallelism 4",
    "dev": "npm run prepare:docs && astro dev",
    "preview": "astro preview",
    "prepare:docs": "node scripts/prepare-docs.mjs",
    "legacy:redirects": "node scripts/generate-legacy-redirects.mjs",
    "audit:output": "node scripts/audit-output.mjs",
    "test": "vitest run",
    "check": "npm run prepare:docs && astro check",
    "build": "npm run prepare:docs && astro build && npm run legacy:redirects && npm run audit:output",
    "validate": "npm run prepare:docs && vitest run && astro check && astro build && npm run legacy:redirects && npm run audit:output"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "7.2.1",
    "@astrojs/starlight": "0.41.3",
    "astro": "7.1.3",
    "pagefind": "1.5.2",
    "remark-directive": "4.0.0",
    "sharp": "0.35.3",
    "starlight-links-validator": "0.25.2",
    "starlight-llms-txt": "0.11.0",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "@types/node": "26.1.1",
    "hast-util-from-html": "2.0.3",
    "remark-parse": "11.0.0",
    "typescript": "6.0.3",
    "unified": "11.0.5",
    "unist-util-visit": "5.1.0",
    "vitest": "4.1.10"
  }
}
